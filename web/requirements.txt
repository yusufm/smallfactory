Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.3.1
blinker==1.9.0

# Sticker generation
pillow
qrcode[pil]

# PDF export
reportlab
 
# Vision (Ollama client + schema)
ollama
pydantic>=2
requests

# Production WSGI server
gunicorn

# ASGI server (used when MCP is integrated into `sf web`)
uvicorn
